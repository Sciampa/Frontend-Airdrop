/* eslint-disable no-empty-pattern */
import { Icon, type IconProps } from "@chakra-ui/react"
import { PiSwapFill } from "react-icons/pi"

export const SwapIconNavMobile = ({}: IconProps) => (
	<Icon boxSize={7}>
		<PiSwapFill />
	</Icon>
)
