/* eslint-disable no-empty-pattern */
import { Icon, type IconProps } from "@chakra-ui/react"
import { GiReactor } from "react-icons/gi"

export const GovernanceIconNavMobile = ({}: IconProps) => (
	<Icon boxSize={8}>
		<GiReactor />
	</Icon>
)
