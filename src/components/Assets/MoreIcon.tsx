/* eslint-disable no-empty-pattern */
import { Icon, type IconProps } from "@chakra-ui/react"
import { HiOutlineSquares2X2 } from "react-icons/hi2"

export const MoreIcon = ({}: IconProps) => (
	<Icon boxSize={7} mr="0.05rem">
		<HiOutlineSquares2X2 />
	</Icon>
)
