/* eslint-disable no-empty-pattern */
import { Icon, type IconProps } from "@chakra-ui/react"
import { FaLayerGroup } from "react-icons/fa"

export const StakingIconNavMobile = ({}: IconProps) => (
	<Icon boxSize={7}>
		<FaLayerGroup />
	</Icon>
)
