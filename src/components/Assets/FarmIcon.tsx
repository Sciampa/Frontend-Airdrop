/* eslint-disable no-empty-pattern */
import { Icon, type IconProps } from "@chakra-ui/react"
import { BiHive } from "react-icons/bi"

export const FarmIcon = ({}: IconProps) => (
	<Icon boxSize={9} mt="0.3rem" ml="0.1rem">
		<BiHive />
	</Icon>
)
