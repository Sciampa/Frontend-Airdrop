/* eslint-disable no-empty-pattern */
import { Icon, type IconProps } from "@chakra-ui/react"
import { GiChaingun } from "react-icons/gi"

export const AssetsIconRouter = ({}: IconProps) => (
	<Icon boxSize={7} mt={1}>
		<GiChaingun />
	</Icon>
)
