/* eslint-disable no-empty-pattern */
import { Icon, type IconProps } from "@chakra-ui/react"
import { FaArtstation } from "react-icons/fa6"

export const BridgeIconNavMobile = ({}: IconProps) => (
	<Icon boxSize={6} mr="0.2rem" mb="0.2rem">
		<FaArtstation />
	</Icon>
)
