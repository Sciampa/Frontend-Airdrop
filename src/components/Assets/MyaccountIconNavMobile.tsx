/* eslint-disable no-empty-pattern */
import { Icon, type IconProps } from "@chakra-ui/react"
import { FaHouseUser } from "react-icons/fa6"

export const MyaccountIconNavMobile = ({}: IconProps) => (
	<Icon boxSize={7}>
		<FaHouseUser />
	</Icon>
)
